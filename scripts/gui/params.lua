local const = {
    ["prefix"] = "FV_", -- Prefix for gui element names to avoid conflicts with other mods
    ["close_button"] = "close_button", -- close button at the top right corner of each window
    ["item_selection"] = "choose_item_button", -- button in item uplink/downlink
    ["udlink_checkbox"] = "uplink_downlink_checkbox",
    ["freq_selection_flow"] = "freq_selection_flow",
}

return const